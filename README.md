# The Rewire Engine

แอปพลิเคชันที่ปรึกษาด้านการเปลี่ยนแปลงธุรกิจ (Business Transformation Strategist) ที่ใช้ AI (Google Gemini) เพื่อช่วยวิเคราะห์และจัดระบบกระบวนการทำงานใหม่

## 🎯 คุณสมบัติหลัก

- **AS-IS-WORKFLOW Analysis**: วิเคราะห์กระบวนการทำงานปัจจุบัน
- **Paste-Point Detection**: ระบุจุดที่นำ AI ไปใช้งาน
- **Strategic Goal Setting**: เลือกเป้าหมายเชิงกลยุทธ์ (Efficiency, CX, Growth)
- **AI-Powered Rewiring**: สร้าง workflow ใหม่โดย AI
- **Helper Features**: ร่าง workflow และแนะนำ paste-point อัตโนมัติ

## 🏗️ โครงสร้างโปรเจค

```text
The Rewire Engine/
├── backend/                 # Node.js + Express API Server
│   ├── server.js           # Main server file
│   ├── package.json        # Backend dependencies
│   ├── .env.example        # ตัวอย่างไฟล์ environment variables
│   └── .gitignore
│
└── frontend/               # Static HTML/CSS/JS
    ├── index.html         # Main HTML file
    └── app.js            # Frontend JavaScript
```

## 🚀 การติดตั้งและใช้งาน

### ข้อกำหนดเบื้องต้น

- Node.js (เวอร์ชัน 16 ขึ้นไป)
- npm หรือ yarn
- Gemini API Key จาก [Google AI Studio](https://aistudio.google.com/app/apikey)

### 1. ติดตั้ง Backend

```powershell
# เข้าไปที่โฟลเดอร์ backend
cd backend

# ติดตั้ง dependencies
npm install

# คัดลอกไฟล์ .env.example และตั้งชื่อเป็น .env
copy .env.example .env

# แก้ไขไฟล์ .env และใส่ API Key ของคุณ
# GEMINI_API_KEY=your_actual_api_key_here
```

### 2. เปิดไฟล์ .env และใส่ API Key

```env
GEMINI_API_KEY=your_gemini_api_key_here
PORT=3000
```

### 3. รัน Backend Server

```powershell
# รันในโหมด production
npm start

# หรือรันในโหมด development (ใช้ nodemon)
npm run dev
```

Backend จะทำงานที่ `http://localhost:3000`

### 4. เปิด Frontend

เปิดไฟล์ `frontend/index.html` ในเบราว์เซอร์ หรือใช้ live server

หรือเปิดด้วย VS Code Simple Browser:

- คลิกขวาที่ไฟล์ `index.html`
- เลือก "Open with Live Server" (ถ้าติดตั้ง Live Server extension)

## 🔧 การปรับแต่ง

### เปลี่ยน Backend URL

ถ้าคุณ deploy backend ที่อื่น แก้ไขที่ไฟล์ `frontend/app.js`:

```javascript
const API_BASE_URL = 'http://localhost:3000'; // เปลี่ยนเป็น URL ของคุณ
```

### เปลี่ยน Port ของ Backend

แก้ไขไฟล์ `backend/.env`:

```env
PORT=5000  # เปลี่ยนเป็นพอร์ตที่ต้องการ
```

## 📡 API Endpoints

### 1. POST `/api/rewire`

วิเคราะห์และสร้าง rewired workflow

**Request Body:**

```json
{
  "asIsWorkflow": "string",
  "pastePoint": "string",
  "strategicGoal": "Efficiency|CX|Growth"
}
```

### 2. POST `/api/draft-workflow`

ร่าง workflow จากคำอธิบาย

**Request Body:**

```json
{
  "prompt": "string"
}
```

### 3. POST `/api/suggest-paste-point`

แนะนำ paste-point จาก workflow

**Request Body:**

```json
{
  "workflow": "string"
}
```

### 4. GET `/health`

ตรวจสอบสถานะเซิร์ฟเวอร์

## 🔒 ความปลอดภัย

✅ **API Key ถูกซ่อนไว้ที่ Backend**: ไม่เปิดเผยใน frontend code  
✅ **CORS Protection**: ป้องกันการเรียกใช้จากโดเมนที่ไม่ได้รับอนุญาต  
✅ **Environment Variables**: เก็บข้อมูลสำคัญใน .env ที่ไม่ถูก commit  
✅ **.gitignore**: ป้องกันไฟล์สำคัญถูกอัปโหลดขึ้น Git

### ⚠️ ข้อควรระวัง

1. **อย่า commit ไฟล์ .env** ขึ้น Git
2. **อย่าแชร์ API Key** กับผู้อื่น
3. ตั้งค่า **CORS** ให้เหมาะสมก่อน deploy จริง
4. ใช้ **HTTPS** เมื่อ deploy production

## 🚀 การ Deploy

### Backend (แนะนำ)

- **Heroku**: รองรับ Node.js โดยตรง
- **Railway**: ใช้งานง่าย มี free tier
- **Render**: รองรับ auto-deploy จาก Git
- **Google Cloud Run**: สำหรับผู้ใช้ Google Cloud

### Frontend

- **Netlify**: ง่ายสำหรับ static site
- **Vercel**: รวดเร็ว รองรับ CI/CD
- **GitHub Pages**: ฟรีสำหรับ public repo

## 📝 License

MIT License - สร้างโดย Wirun Wetsiri (Pharm Connection Co., Ltd.)

## 🙏 Credits

**Application Concept by**: Wirun Wetsiri  
**Company**: [Pharm Connection Co., Ltd.](https://www.pharmconnection.net)  
**Powered by**: Google Gemini AI

---

## 💡 Tips

- ใช้ฟีเจอร์ "ร่าง Workflow" เพื่อเริ่มต้นอย่างรวดเร็ว
- ให้ AI แนะนำ Paste-Point เพื่อหาจุดที่เหมาะสม
- เลือก Strategic Goal ให้ตรงกับเป้าหมายธุรกิจของคุณ
- ผลลัพธ์จาก AI ควรได้รับการตรวจสอบก่อนนำไปใช้จริง
