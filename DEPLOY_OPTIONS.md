# Vercel Deployment (Full Stack)

## ⚡ Deploy ทั้ง Frontend + Backend บน Vercel (แนะนำถ้าต้องการที่เดียวจบ)

Vercel รองรับ Serverless Functions สามารถรัน Node.js backend ได้

## ขั้นตอน

### 1. ปรับโครงสร้างโปรเจค

สร้างโฟลเดอร์ `api` สำหรับ Serverless Functions:

```
rewire/
├── api/              # Backend Serverless Functions
│   └── (จะสร้างใหม่)
├── frontend/         # Static Files
└── vercel.json      # Config
```

### 2. สร้างไฟล์ vercel.json

ใส่ config นี้ที่ root ของโปรเจค:

```json
{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.js",
      "use": "@vercel/node"
    },
    {
      "src": "frontend/**",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/frontend/$1"
    }
  ]
}
```

### 3. แปลง Backend เป็น Serverless Functions

แต่ละ API endpoint จะเป็นไฟล์แยก ใน `api/` folder

**ข้อจำกัด**: ต้องเขียน code ใหม่เพื่อให้เป็น serverless

---

## 🎯 คำแนะนำ

**สำหรับโปรเจคนี้ ผมแนะนำ:**

### ✅ ตัวเลือกที่ 1: Railway + Netlify
- **ง่ายที่สุด** - ไม่ต้องแก้ code
- Deploy ได้เลย
- ฟรี

### ✅ ตัวเลือกที่ 2: Render + Netlify
- ทางเลือกสำรอง
- ฟรีเหมือนกัน
- แต่ service จะหลับเมื่อไม่ได้ใช้

### ⚠️ ตัวเลือกที่ 3: Vercel Full Stack
- ต้องแก้ code backend ใหม่ทั้งหมด
- เหมาะสำหรับคนที่มีประสบการณ์
- ไม่แนะนำถ้าเพิ่งเริ่มต้น

---

## 💰 เปรียบเทียบราคา (Free Tier)

| Platform | Free Limit | ข้อดี | ข้อเสียหา |
|----------|------------|-------|----------|
| **Railway** | 500 ชม./เดือน | ง่าย, เร็ว, ไม่หลับ | จำกัดชั่วโมง |
| **Render** | 750 ชม./เดือน | ฟรีนานกว่า | Service จะหลับ |
| **Netlify** | ไม่จำกัด | ฟรีเต็มที่ | Frontend อย่างเดียว |
| **Vercel** | ไม่จำกัด | ทั้ง Frontend+Backend | ต้องแก้ code |

---

## 🚀 ขั้นตอนที่แนะนำ (Railway + Netlify)

1. Deploy Backend บน Railway (ตาม DEPLOY_RAILWAY.md)
2. Deploy Frontend บน Netlify
3. เปลี่ยน API_BASE_URL ใน frontend/app.js
4. Push ขึ้น GitHub → Auto-deploy

**ใช้เวลาไม่เกิน 10 นาที!**
