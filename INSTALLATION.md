# ขั้นตอนการติดตั้งและใช้งาน The Rewire Engine

## 🎯 สำหรับผู้ใช้ทั่วไป (ไม่มีความรู้ด้าน Programming)

### ขั้นตอนที่ 1: ติดตั้ง Node.js

1. เปิด browser ไปที่ <https://nodejs.org/>
2. ดาวน์โหลดเวอร์ชัน LTS (แนะนำ)
3. ติดตั้งโดยคลิก Next ไปเรื่อยๆ
4. เปิด PowerShell หรือ Command Prompt แล้วพิมพ์:

   ```bash
   node --version
   ```

   ถ้าขึ้นเลขเวอร์ชัน แสดงว่าติดตั้งสำเร็จ

### ขั้นตอนที่ 2: ขอ API Key จาก Google

1. เปิด browser ไปที่ <https://aistudio.google.com/app/apikey>
2. Login ด้วย Google Account
3. คลิก "Create API Key"
4. คัดลอก API Key ที่ได้ (อย่าแชร์กับใคร!)

### ขั้นตอนที่ 3: ตั้งค่าโปรเจค

1. เปิดโฟลเดอร์ `The Rewire Engine` ที่ Desktop
2. เข้าไปในโฟลเดอร์ `backend`
3. คลิกขวาที่พื้นที่ว่าง เลือก "Open in Terminal" หรือ "Open PowerShell window here"
4. พิมพ์คำสั่ง:

   ```bash
   npm install
   ```

   (รอจนเสร็จ อาจใช้เวลา 1-2 นาที)

### ขั้นตอนที่ 4: ใส่ API Key

1. ในโฟลเดอร์ `backend` หาไฟล์ชื่อ `.env.example`
2. คัดลอกไฟล์นี้และเปลี่ยนชื่อเป็น `.env` (เอา .example ออก)
3. เปิดไฟล์ `.env` ด้วย Notepad
4. เปลี่ยน `your_gemini_api_key_here` เป็น API Key ที่คุณได้จาก Google
5. บันทึกและปิดไฟล์

ตัวอย่าง:

```env
GEMINI_API_KEY=AIzaSyABC123...xyz789
PORT=3000
```

### ขั้นตอนที่ 5: เปิดใช้งาน Backend

1. เปิด PowerShell/Terminal ในโฟลเดอร์ `backend` (ถ้าปิดไปแล้ว)
2. พิมพ์:

   ```bash
   npm start
   ```

3. เมื่อเห็นข้อความ "<http://localhost:3000>" แสดงว่าพร้อมใช้งาน
4. **อย่าปิดหน้าต่างนี้** (ให้ Backend ทำงานอยู่เรื่อยๆ)

### ขั้นตอนที่ 6: เปิด Frontend

#### วิธีที่ 1: เปิดไฟล์โดยตรง (แนะนำ)

1. ไปที่โฟลเดอร์ `frontend`
2. ดับเบิลคลิกที่ไฟล์ `index.html`
3. เปิดด้วย Browser (Chrome, Edge, Firefox)

#### วิธีที่ 2: ใช้ VS Code Live Server (สำหรับผู้ที่มี VS Code)

1. เปิดโฟลเดอร์ `The Rewire Engine` ใน VS Code
2. คลิกขวาที่ `frontend/index.html`
3. เลือก "Open with Live Server"

### ขั้นตอนที่ 7: ใช้งาน

1. หน้าเว็บจะเปิดขึ้นมา
2. กรอกข้อมูลในช่อง:
   - AS-IS-WORKFLOW: กระบวนการทำงานปัจจุบัน
   - PASTE-POINT: จุดที่ใช้ AI
   - เลือกเป้าหมาย: Efficiency, CX, หรือ Growth
3. คลิก "วิเคราะห์และจัดระบบใหม่ (Rewire)"
4. รอผลลัพธ์

---

## 🛠️ สำหรับ Developers

### Quick Start

```powershell
# 1. Clone หรือดาวน์โหลดโปรเจค

# 2. ติดตั้ง Backend
cd backend
npm install
copy .env.example .env
# แก้ไข .env ใส่ API Key

# 3. รัน Backend
npm start

# 4. เปิด Frontend
# เปิด frontend/index.html ใน browser หรือใช้ live server
```

### Development Mode

```powershell
# Backend with auto-restart
cd backend
npm run dev
```

### การปรับแต่ง

#### เปลี่ยน Backend URL

แก้ไขในไฟล์ `frontend/app.js`:
```javascript
const API_BASE_URL = 'http://your-backend-url.com';
```

#### เปลี่ยน Port

แก้ไขในไฟล์ `backend/.env`:
```
PORT=5000
```

#### ปรับแต่ง CORS

แก้ไขในไฟล์ `backend/server.js`:
```javascript
app.use(cors({
  origin: 'https://your-frontend-domain.com'
}));
```

---

## 🚨 แก้ไขปัญหา (Troubleshooting)

### ปัญหา: "npm: command not found"

**วิธีแก้**: ติดตั้ง Node.js ใหม่จาก <https://nodejs.org/>

### ปัญหา: "GEMINI_API_KEY is not set"

**วิธีแก้**:

1. ตรวจสอบว่าไฟล์ `.env` มีอยู่ในโฟลเดอร์ `backend`
2. ตรวจสอบว่าใส่ API Key แล้ว
3. ลอง restart backend ใหม่

### ปัญหา: "Failed to fetch" หรือ "CORS Error"

**วิธีแก้**:

1. ตรวจสอบว่า Backend ทำงานอยู่ (ดูที่ Terminal)
2. ตรวจสอบ URL ใน `frontend/app.js` ว่าตรงกับ Backend
3. เปิด Frontend จาก `http://` หรือ `https://` (ไม่ใช่ `file://`)

### ปัญหา: "API Error 400/500"

**วิธีแก้**:

1. ตรวจสอบว่ากรอกข้อมูลครบทุกช่อง
2. ลอง refresh หน้าเว็บและลองใหม่
3. ตรวจสอบ API Key ว่ายังใช้งานได้

---

## 📞 ติดต่อ

หากมีปัญหาหรือข้อสงสัย:

- ดู Log ที่ Terminal/PowerShell ที่รัน Backend
- ดู Console ของ Browser (กด F12)
- ติดต่อ: Wirun Wetsiri - [Pharm Connection Co., Ltd.](https://www.pharmconnection.net)
